# Deployment configuration for MTN SRE Assessment
# Fill these values and run configure-environment.ps1

# Elastic APM (OTel) endpoint
# Use Elastic Cloud APM URL (e.g. https://<your-apm-host>:443 or https://<your-apm-subdomain>.apm.<region>.elastic.cloud)
ELASTIC_APM_ENDPOINT="https://mtn-sre-assessment-observability-fece91.apm.us-central1.gcp.elastic.cloud:443"
ELASTIC_APM_TOKEN="VTA3MEY1MEJOY1NvalI1MFkyWEY6RU1DTXk5RmNXSS13MWFyMVlKbTRnUQ=="

# Legacy variables (optional)
# ELASTIC_APM_SERVER="my-elasticsearch-project-d1eb4f.es.europe-west3.gcp.elastic.cloud:443"

# PostgreSQL host (for Elastic Agent policy and app config)
POSTGRES_HOST="34.123.59.20"
POSTGRES_PORT="5432"

# Redis host
REDIS_HOST="10.2.241.251"
REDIS_PORT="6379"

# Optional: APM Server protocol (if needed by other templates)
ELASTIC_APM_PROTOCOL="https"

# GKE config variables (not used directly in collector YAML but for reference)
GKE_CLUSTER_NAME="my-gke-cluster"
GKE_PROJECT="mtn-sre-test-case"
GKE_ZONE="us-central1-a"

# Optional container registry
CONTAINER_REGISTRY="gcr.io/mtn-sre-test-case"